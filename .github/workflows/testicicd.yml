name: Deploy Hello World to EB

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '22'

      - name: Install Dependencies
        run: npm install

      - name: Package for Elastic Beanstalk
        run: zip -r eb-deploy.zip . -x "*.git*" "node_modules/*"

      - name: Deploy to Elastic Beanstalk
        uses: einaregilsson/beanstalk-deploy@v22
        with:
          aws_access_key: ${{ secrets.AWS_ACCESS_KEY }}
          aws_secret_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          application_name: 'Reko'
          environment_name: 'Reko-env'
          region: 'eu-north-1'
          deployment_package: eb-deploy.zip
          use_existing_version_if_available: false




  # deploy-backend:
  #   name: Deploy Backend
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout Source
  #       uses: actions/checkout@v4

  #     - name: Set up Node.js (tai muu runtime)
  #       uses: actions/setup-node@v3
  #       with:
  #         node-version: '22'

  #     - name: Install Backend Dependencies
  #       working-directory: ./backend
  #       run: npm install

  #     - name: Run Backend Tests
  #       working-directory: ./backend
  #       run: npm test || echo "No backend tests defined"

  #     - name: Generate Backend Deployment Package
  #       run: |
  #         cd backend
  #         zip -r ../backend-build.zip . -x "*.git*" "node_modules/*"

  #     - name: Deploy Backend to Elastic Beanstalk
  #       uses: einaregilsson/beanstalk-deploy@v22
  #       with:
  #         aws_access_key: ${{ secrets.AWS_ACCESS_KEY }}
  #         aws_secret_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  #         application_name: 'Reko'
  #         environment_name: 'Reko-env'
  #         version_label: ${{ github.sha }}
  #         region: eu-north-1
  #         deployment_package: backend-build.zip
